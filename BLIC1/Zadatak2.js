var str = "javascript i nije toliko los"

function vrati(x) {
    var b = x / 2
    var a = Math.ceil(b)
    for (let index = x.a; index < x.length; index++) {
        console.log(x.index)

    }
}
vrati(str)